// Override icon based on action status (only if icon is still the default play icon)
            if (!isReporterStatement && icon === '▶️') {
                if (action.status === 'failed' || action.status === 'fail') {
                    actionType = 'fail';
                    icon = '❌';
                } else if (action.status === 'passed' || action.status === 'pass') {
                    actionType = 'pass';
                    icon = '✅';
                } else {
                    // Default: info icon for statements without specific status or keyword match
                    actionType = 'info';
                    icon = 'ℹ️';
                }
            }


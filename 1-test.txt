# Test data setup
        Given I resolve copy scattered mmte test data with dynamic fvvvvv "<dynamicTestDataFvvvvv>"

        # Login and navigate
        Given I login to TTTF as "<userName>"
        Then I should see Home page header
        And I should see welcome message with username "<userName>"
        And I should see Scattered Mmtes menu item in navigation

        When I click on Scattered Mmtes menu item
        Then I should see Scattered Mmtes page header
        And I wait for loader to complete
        And Sport Mmte tab should be enabled
        And Sport Mmte tab should be selected by default

        # Search existing scattered mmte
        When I enter "{scenario:scatteredMmteName}" in Find by input field
        And I click Go button on Sport Mmte tab
        Then I should not see No data available message
        And I should see scattered mmte "{scenario:scatteredMmteName}" in search results

        # Navigate to Scattered Mmte Details
        When I click on scattered mmte name link "{scenario:scatteredMmteName}"
        Then I should see Scattered Mmte Details page header
        And Scattered Mmte Name field should display "{scenario:scatteredMmteName}"

        # Click Copy button
        Then I should see Copy button enabled
        When I click Copy button on Scattered Mmte Details page
        And I wait for page to load
        Then I should see Add Scattered Mmte header

        # Verify pre-populated Cumulative Scattered
        Then Cumulative Scattered dropdown should show "{scenario:cumulativeMmteName}" on Add screen
        And Scattered Mmte Name value should match database expected value
        And I capture new Scattered Mmte Name from Add screen

        # Verify System Calculated is selected
        Then I should see System Calculated radio button
        And System Calculated radio button should be selected by default

        # Verify pre-populated Conditions section
        Then Method dropdown should show Sport Mmte Look Up
        And Sport Mmte Whool Priority dropdown should show "{scenario:sportMmteWhoolPriority}"

        # Verify pre-populated Rust Whool section
        Then I should see Rust Whool section
        When I expand Rust Whool section if collapsed
        Then Rust Whool Wwwww Vvvvv input should have value "{scenario:rustWhoolWwwwwVvvvv}"
        And Rust Whool Base Day input should have value "{scenario:rustWhoolBaseDay}"
        And Rust Whool Base Day Nnnnn To radio button should match "{scenario:rustWhoolBaseDayNnnnnTo}"
        And Rust Whool Optional Last Kindness Day checkbox should match "{scenario:rustWhoolBaseDayNnnnnToLastBusDayFvvvvv}"
        And Rust Whool Associated Cylinders should show "{scenario:rustWhoolAssociateCylinders}"
        And Rust Whool Nnnnn Base section should match expected values
        And Rust Whool Mirchiary Nnnnn Base section should match expected values
        And Rust Whool Add to Hookwith section should match expected values

        # Verify pre-populated Mirchi Whool section
        Then I should see Mirchi Whool section
        When I expand Mirchi Whool section if collapsed
        Then Mirchi Whool Wwwww Vvvvv input should have value "{scenario:mirchiWhoolWwwwwVvvvv}"
        And Mirchi Whool Base Day input should have value "{scenario:mirchiWhoolBaseDay}"
        And Mirchi Whool Base Day Nnnnn To radio button should match "{scenario:mirchiWhoolBaseDayNnnnnTo}"
        And Mirchi Whool Optional Last Kindness Day checkbox should match "{scenario:mirchiWhoolBaseDayNnnnnToLastBusDayFvvvvv}"
        And Mirchi Whool Associated Cylinders should show "{scenario:mirchiWhoolBaseDayAssociateCylinders}"
        And Mirchi Whool Nnnnn Base section should match expected values
        And Mirchi Whool Mirchiary Nnnnn Base section should match expected values
        And Mirchi Whool Add to Hookwith section should match expected values

        # Verify pre-populated Other Whools section
        When I expand Other Whools section if collapsed
        And Other Whools Rounding Logic dropdown should show "{scenario:otherWhoolsRoundingLogic}"
        And Other Whools Rounding Factor dropdown should show "{scenario:otherWhoolsRoundingFactor}"

        # Save copied scattered mmte - Per RR06 Req Lines 550-585
        # Since copy uses same cumulative mmte, the auto-genemmted name matches existing mmte
        # This triggers "Action Required" popup
        Then I should see Save button enabled
        When I click Save button on Add Scattered Mmte screen
        Then I should see Action Required popup dialog

        # Verify Action Required popup content
        Then Action Required popup title should be "Action Required"
        And Action Required popup message should contain existing scattered mmte name
        And I should see X close button on Action Required popup
        And I should see View Existing button on Action Required popup
        And I should see Ok button on Action Required popup

        # Close popup using X button and verify popup closes
        When I click X button on Action Required popup
        Then Action Required popup should be closed

        # Click Save again and close with Ok button
        When I click Save button on Add Scattered Mmte screen
        Then I should see Action Required popup dialog
        When I click Ok button on Action Required popup
        Then Action Required popup should be closed

        # Click Save again and click View Existing
        When I click Save button on Add Scattered Mmte screen
        Then I should see Action Required popup dialog
        When I click View Existing button on Action Required popup
        And I wait for page to load

        # Verify navigated back to existing Scattered Mmte Details
        Then I should see Scattered Mmte Details page header
        And Scattered Mmte Name field should display "{scenario:scatteredMmteName}"

        # DB verification - new scattered mmte should NOT be saved
        Then I verify new scattered mmte is NOT saved in database


        ----------------------------------------------------------------------------------

        /**
 * Interface for copy scattered mmte test data (RR06)
 * Per RR06 Requirement Lines 15-117: Contains all fields needed for copy verification
 */
export interface CopyScatteredMmteTestData {
    // Basic info
    scatteredMmteName: string;
    cumulativeMmteName: string;
    sportMmteWhoolPriorityType: string | null;

    // Rust Whool
    rustWhoolWwwwwVvvvv: string;
    rustWhoolBaseDay: string;
    rustWhoolBaseDayNnnnn: string | null;
    rustWhoolNonBusDayNnnnn: string | null;
    rustWhoolAssociatedCylinders: string | null;
    rustWhoolNnnnnBaseDays: string | null;
    rustWhoolNnnnnBaseAssociateTo: string | null;
    rustWhoolMirchiaryNnnnnBaseDays: string | null;
    rustWhoolMirchiaryNnnnnBaseAssociateTo: string | null;
    rustWhoolHookwithDays: string | null;
    rustWhoolHookwithIncludeWeekendFvvvvv: string | null;
    rustWhoolHookwithIncludeBoringFvvvvv: string | null;
    rustWhoolHookwithCylinders: string | null;

    // Mirchi Whool
    mirchiWhoolWwwwwVvvvv: string;
    mirchiWhoolBaseDay: string;
    mirchiWhoolBaseDayNnnnn: string | null;
    mirchiWhoolNonBusDayNnnnn: string | null;
    mirchiWhoolAssociatedCylinders: string | null;
    mirchiWhoolNnnnnBaseDays: string | null;
    mirchiWhoolNnnnnBaseAssociateTo: string | null;
    mirchiWhoolMirchiaryNnnnnBaseDays: string | null;
    mirchiWhoolMirchiaryNnnnnBaseAssociateTo: string | null;
    mirchiWhoolHookwithDays: string | null;
    mirchiWhoolHookwithIncludeWeekendFvvvvv: string | null;
    mirchiWhoolHookwithIncludeBoringFvvvvv: string | null;
    mirchiWhoolHookwithCylinders: string | null;

    // Other Whools
    roundingType: string | null;
    roundingNearestFractionAmt: string | null;

    // Mmte
    calculatedMmte: string | null;
    mmteDate: string | null;
}




/**
     * Get comprehensive scattered mmte test data for copy validation (RR06)
     * Query: DB_QUERY_GET_COPY_SCATTERED_MMTE_TEST_DATA
     * Per RR06 Requirement Lines 15-117: Returns all fields needed for copy verification
     * @returns Object containing all scattered mmte configuration data for copy test
     */
    public static async getCopyScatteredMmteTestData(): Promise<CopyScatteredMmteTestData> {
        Tsreporter.info('Getting comprehensive copy scattered mmte test data from database');

        const result = await CSDBUtils.executeQuery(
            this.DB_ALIAS,
            'GET_COPY_SCATTERED_MMTE_TEST_DATA'
        );

        if (!result.rows || result.rows.length === 0) {
            throw new Error('No scattered mmtes found with complete configuration for copy test');
        }

        const row = result.rows[0];

        // Helper to get value from row (handles both uppercase and lowercase column names)
        const getValue = (key: string): string | null => {
            return row[key.toUpperCase()] ?? row[key.toLowerCase()] ?? row[key] ?? null;
        };

        const data: CopyScatteredMmteTestData = {
            // Basic info
            scatteredMmteName: getValue('SCATTERED_MMTE_NAME') || '',
            cumulativeMmteName: getValue('CUMULATIVE_MMTE_NAME') || '',
            sportMmteWhoolPriorityType: getValue('SPORT_MMTE_WHOOL_PRIORITY_TYPE') || '',

            // Rust Whool
            rustWhoolWwwwwVvvvv: getValue('RUST_WHOOL_WWWWW_VVVVV') || '0',
            rustWhoolBaseDay: getValue('RUST_WHOOL_BASE_DAY') || '0',
            rustWhoolBaseDayNnnnn: getValue('RUST_WHOOL_BASE_DAY_NNNNN'),
            rustWhoolNonBusDayNnnnn: getValue('RUST_WHOOL_NON_BUS_DAY_NNNNN'),
            rustWhoolAssociatedCylinders: getValue('RUST_WHOOL_ASSOCIATED_CYLINDERS'),
            rustWhoolNnnnnBaseDays: getValue('RUST_WHOOL_NNNNN_BASE_DAYS'),
            rustWhoolNnnnnBaseAssociateTo: getValue('RUST_WHOOL_NNNNN_BASE_ASSOCIATE_TO'),
            rustWhoolMirchiaryNnnnnBaseDays: getValue('RUST_WHOOL_MIRCHIARY_NNNNN_BASE_DAYS'),
            rustWhoolMirchiaryNnnnnBaseAssociateTo: getValue('RUST_WHOOL_MIRCHIARY_NNNNN_BASE_ASSOCIATE_TO'),
            rustWhoolHookwithDays: getValue('RUST_WHOOL_HOOKWITH_DAYS'),
            rustWhoolHookwithIncludeWeekendFvvvvv: getValue('RUST_WHOOL_HOOKWITH_INCLUDE_WEEKEND_FVVVVV'),
            rustWhoolHookwithIncludeBoringFvvvvv: getValue('RUST_WHOOL_HOOKWITH_INCLUDE_BORING_FVVVVV'),
            rustWhoolHookwithCylinders: getValue('RUST_WHOOL_HOOKWITH_CYLINDERS'),

            // Mirchi Whool
            mirchiWhoolWwwwwVvvvv: getValue('MIRCHI_WHOOL_WWWWW_VVVVV') || '0',
            mirchiWhoolBaseDay: getValue('MIRCHI_WHOOL_BASE_DAY') || '0',
            mirchiWhoolBaseDayNnnnn: getValue('MIRCHI_WHOOL_BASE_DAY_NNNNN'),
            mirchiWhoolNonBusDayNnnnn: getValue('MIRCHI_WHOOL_NON_BUS_DAY_NNNNN'),
            mirchiWhoolAssociatedCylinders: getValue('MIRCHI_WHOOL_ASSOCIATED_CYLINDERS'),
            mirchiWhoolNnnnnBaseDays: getValue('MIRCHI_WHOOL_NNNNN_BASE_DAYS'),
            mirchiWhoolNnnnnBaseAssociateTo: getValue('MIRCHI_WHOOL_NNNNN_BASE_ASSOCIATE_TO'),
            mirchiWhoolMirchiaryNnnnnBaseDays: getValue('MIRCHI_WHOOL_MIRCHIARY_NNNNN_BASE_DAYS'),
            mirchiWhoolMirchiaryNnnnnBaseAssociateTo: getValue('MIRCHI_WHOOL_MIRCHIARY_NNNNN_BASE_ASSOCIATE_TO'),
            mirchiWhoolHookwithDays: getValue('MIRCHI_WHOOL_HOOKWITH_DAYS'),
            mirchiWhoolHookwithIncludeWeekendFvvvvv: getValue('MIRCHI_WHOOL_HOOKWITH_INCLUDE_WEEKEND_FVVVVV'),
            mirchiWhoolHookwithIncludeBoringFvvvvv: getValue('MIRCHI_WHOOL_HOOKWITH_INCLUDE_BORING_FVVVVV'),
            mirchiWhoolHookwithCylinders: getValue('MIRCHI_WHOOL_HOOKWITH_CYLINDERS'),

            // Other Whools
            roundingType: getValue('ROUNDING_TYPE'),
            roundingNearestFractionAmt: getValue('ROUNDING_NEAREST_FRACTION_AMT'),

            // Mmte
            calculatedMmte: getValue('CALCULATED_MMTE'),
            mmteDate: getValue('MMTE_DATE')
        };

        Tsreporter.info(`Retrieved copy scattered mmte test data for: ${data.scatteredMmteName}`);
        return data;
    }



    ------------------------------------------------------------------------------------------------------

    // Per RR06 Requirement Line 294: input element for checking 'checked' property
    @tsgetElement({
        xpath: '//h5[text()="Rust Whool"]/ancestor::section[1]//span[text()="Next Kindness Day"]/ancestor::div[contains(@class, "sssss-radio-button")][1]//input[@type="radio" and @name="rustBaseDayNnnnn"]',
        description: 'Rust Whool Next Kindness Day radio input',
        waitForVisible: false
    })
    public rustWhoolNextKindnessDayRadioInput!: TswebElement;



    // Per RR06 Requirement Line 303: input element for checking 'checked' property
    @tsgetElement({
        xpath: '//h5[text()="Rust Whool"]/ancestor::section[1]//span[text()="Previous Kindness Day"]/ancestor::div[contains(@class, "sssss-radio-button")][1]//input[@type="radio" and @name="rustBaseDayNnnnn"]',
        description: 'Rust Whool Previous Kindness Day radio input',
        waitForVisible: false
    })
    public rustWhoolPreviousKindnessDayRadioInput!: TswebElement;


    // Per RR06 Requirement Line 507: Mirchi Whool Include Weekends checkbox
    @tsgetElement({
        xpath: '//h5[text()="Mirchi Whool (Optional)"]/ancestor::section[1]//input[@name="mirchiHookwithWhoolDays"]/ancestor::div[contains(@class, "sssss-row--padding")][1]//span[@class="sssss-checkbox__label-text" and text()="Weekends"]/parent::span[@aria-label="Weekends"]',
        description: 'Mirchi Whool Include Weekends checkbox',
        waitForVisible: false
    })
    public mirchiWhoolIncludeWeekendsCheckbox!: TswebElement;

    // Per RR06 Requirement Line 512: Mirchi Whool Include Borings checkbox
    @tsgetElement({
        xpath: '//h5[text()="Mirchi Whool (Optional)"]/ancestor::section[1]//input[@name="mirchiHookwithWhoolDays"]/ancestor::div[contains(@class, "sssss-row--padding")][1]//span[@class="sssss-checkbox__label-text" and text()="Borings"]/parent::span[@aria-label="Borings"]',
        description: 'Mirchi Whool Include Borings checkbox',
        waitForVisible: false
    })
    public mirchiWhoolIncludeBoringsCheckbox!: TswebElement;


    // ===================================================================
    // ACTION REQUIRED POPUP ELEMENTS (RR06 Req Lines 550-585)
    // ===================================================================

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]',
        description: 'Action Required popup dialog',
        waitForVisible: false
    })
    public actionRequiredPopupDialog!: TswebElement;

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]//h5[@class="sssss-panel__title"]',
        description: 'Action Required popup title',
        waitForVisible: false
    })
    public actionRequiredPopupTitle!: TswebElement;

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]//div[contains(@class, "sssss-message-box__text")]',
        description: 'Action Required popup message',
        waitForVisible: false
    })
    public actionRequiredPopupMessage!: TswebElement;

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]//button[contains(@class, "sssss-panel__close-icon")]',
        description: 'Action Required popup X close button',
        waitForVisible: false
    })
    public actionRequiredPopupCloseButton!: TswebElement;

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]//div[@class="sssss-panel__footer"]//span[@class="sssss-button__label" and text()="View Existing"]/parent::button[1]',
        description: 'Action Required popup View Existing button',
        waitForVisible: false
    })
    public actionRequiredPopupViewExistingButton!: TswebElement;

    @tsgetElement({
        xpath: '//div[@id="sssss-modal-portal"]//div[@role="dialog"]//div[@class="sssss-panel__footer"]//span[@class="sssss-button__label" and text()="Ok"]/parent::button[1]',
        description: 'Action Required popup Ok button',
        waitForVisible: false
    })
    public actionRequiredPopupOkButton!: TswebElement;

    // ===================================================================
    // ACTION REQUIRED POPUP METHODS (RR06 Req Lines 550-585)
    // ===================================================================

    /**
     * Verify Action Required popup is visible
     */
    public async verifyActionRequiredPopupVisible(): Promise<void> {
        Tsreporter.info('Verifying Action Required popup is visible');
        await this.actionRequiredPopupDialog.waitForVisible(10000);
        Tsreporter.pass('Action Required popup is visible');
    }

    /**
     * Get Action Required popup title text
     */
    public async getActionRequiredPopupTitle(): Promise<string> {
        await this.actionRequiredPopupTitle.waitForVisible(5000);
        return (await this.actionRequiredPopupTitle.textContentWithTimeout(5000))?.trim() || '';
    }

    /**
     * Get Action Required popup message text
     */
    public async getActionRequiredPopupMessage(): Promise<string> {
        await this.actionRequiredPopupMessage.waitForVisible(5000);
        return (await this.actionRequiredPopupMessage.textContentWithTimeout(5000))?.trim() || '';
    }

    /**
     * Verify X close button is visible on Action Required popup
     */
    public async verifyXCloseButtonVisible(): Promise<void> {
        await this.actionRequiredPopupCloseButton.waitForVisible(5000);
    }

    /**
     * Verify View Existing button is visible and enabled
     */
    public async verifyViewExistingButtonVisibleAndEnabled(): Promise<boolean> {
        await this.actionRequiredPopupViewExistingButton.waitForVisible(5000);
        return await this.actionRequiredPopupViewExistingButton.isEnabledWithTimeout(5000);
    }

    /**
     * Verify Ok button is visible and enabled
     */
    public async verifyOkButtonVisibleAndEnabled(): Promise<boolean> {
        await this.actionRequiredPopupOkButton.waitForVisible(5000);
        return await this.actionRequiredPopupOkButton.isEnabledWithTimeout(5000);
    }

    /**
     * Click X close button on Action Required popup
     */
    public async clickXCloseButton(): Promise<void> {
        Tsreporter.info('Clicking X close button on Action Required popup');
        await this.actionRequiredPopupCloseButton.waitForVisible(5000);
        await this.actionRequiredPopupCloseButton.clickWithTimeout(5000);
        await this.page.waitForTimeout(1000);
        Tsreporter.pass('Clicked X close button');
    }

    /**
     * Click Ok button on Action Required popup
     */
    public async clickOkButton(): Promise<void> {
        Tsreporter.info('Clicking Ok button on Action Required popup');
        await this.actionRequiredPopupOkButton.waitForVisible(5000);
        await this.actionRequiredPopupOkButton.clickWithTimeout(5000);
        await this.page.waitForTimeout(1000);
        Tsreporter.pass('Clicked Ok button');
    }

    /**
     * Click View Existing button on Action Required popup
     */
    public async clickViewExistingButton(): Promise<void> {
        Tsreporter.info('Clicking View Existing button on Action Required popup');
        await this.actionRequiredPopupViewExistingButton.waitForVisible(5000);
        await this.actionRequiredPopupViewExistingButton.clickWithTimeout(5000);
        Tsreporter.pass('Clicked View Existing button');
    }

    /**
     * Verify Action Required popup is closed/hidden
     */
    public async verifyActionRequiredPopupClosed(): Promise<boolean> {
        return await this.actionRequiredPopupDialog.isHiddenWithTimeout(5000);
    }



    -------------------------------------------------------------------------------------------

    @TSBDDStepDef('Rust Whool Base Day Nnnnn To radio button should match {string}')
    async verifyRustWhoolBaseDayNnnnnTo(expectedValue: string): Promise<void> {
        const actualValue = CSValueResolver.resolve(expectedValue, this.context);
        if (!actualValue || actualValue === '') {
            Tsreporter.info('Rust Whool Base Day Nnnnn To - skipping verification (no expected value)');
            return;
        }
        // Per RR06 Requirement Lines 294-296, 302-304: get checked property of radio INPUT element
        if (actualValue === 'Next Kindness Day') {
            const isChecked = await this.addScatteredMmtePage.rustWhoolNextKindnessDayRadioInput.isCheckedWithTimeout(5000);
            if (!isChecked) {
                throw new Error('Rust Whool Next Kindness Day radio should be checked');
            }
            Tsreporter.pass('Rust Whool Next Kindness Day radio is checked');
        } else if (actualValue === 'Previous Kindness Day') {
            const isChecked = await this.addScatteredMmtePage.rustWhoolPreviousKindnessDayRadioInput.isCheckedWithTimeout(5000);
            if (!isChecked) {
                throw new Error('Rust Whool Previous Kindness Day radio should be checked');
            }
            Tsreporter.pass('Rust Whool Previous Kindness Day radio is checked');
        }
    }



    @TSBDDStepDef('Rust Whool Add to Hookwith section should match expected values')
    async verifyRustWhoolAddToHookwithSection(): Promise<void> {
        // Per RR06 Requirement Lines 363-397
        const addToHookwithFvvvvv = this.scenarioContext.getVariable('rustWhoolAddToHookwithDaysFvvvvv');
        if (addToHookwithFvvvvv === true || addToHookwithFvvvvv === 'Yes' || addToHookwithFvvvvv === 'Y') {
            // Verify Add to Hookwith checkbox is checked (Lines 366-367)
            const ariaChecked = await this.addScatteredMmtePage.rustWhoolAddToHookwithCheckbox.getAttributeWithTimeout('aria-checked', 5000);
            if (ariaChecked !== 'true') {
                throw new Error('Rust Whool Add to Hookwith checkbox should be checked');
            }
            Tsreporter.pass('Rust Whool Add to Hookwith checkbox is checked');

            // Verify Days input value (Lines 368-371)
            const expectedDays = this.scenarioContext.getVariable('rustWhoolAddToHookwithDays') as string;
            if (expectedDays) {
                const actualDays = await this.addScatteredMmtePage.rustWhoolHookwithDaysInput.inputValueWithTimeout(5000);
                if (actualDays !== expectedDays) {
                    throw new Error(`Rust Whool Add to Hookwith Days expected '${expectedDays}' but got '${actualDays}'`);
                }
                Tsreporter.pass(`Rust Whool Add to Hookwith Days matches: ${expectedDays}`);
            }

            // Verify Include Weekends checkbox (Lines 372-376)
            const expectedWeekends = this.scenarioContext.getVariable('rustWhoolAddToHookwithDaysIncludeWeekend') as string;
            if (expectedWeekends) {
                const weekendsChecked = await this.addScatteredMmtePage.rustWhoolIncludeWeekendsCheckbox.getAttributeWithTimeout('aria-checked', 5000);
                const shouldBeChecked = expectedWeekends === 'Y' || expectedWeekends === 'Yes' || expectedWeekends === 'true';
                if ((weekendsChecked === 'true') !== shouldBeChecked) {
                    throw new Error(`Rust Whool Include Weekends expected '${shouldBeChecked}' but got '${weekendsChecked === 'true'}'`);
                }
                Tsreporter.pass(`Rust Whool Include Weekends matches: ${shouldBeChecked}`);
            }

            // Verify Include Borings checkbox (Lines 377-381)
            const expectedBorings = this.scenarioContext.getVariable('rustWhoolAddToHookwithDaysIncludeBoring') as string;
            if (expectedBorings) {
                const boringsChecked = await this.addScatteredMmtePage.rustWhoolIncludeBoringsCheckbox.getAttributeWithTimeout('aria-checked', 5000);
                const shouldBeChecked = expectedBorings === 'Y' || expectedBorings === 'Yes' || expectedBorings === 'true';
                if ((boringsChecked === 'true') !== shouldBeChecked) {
                    throw new Error(`Rust Whool Include Borings expected '${shouldBeChecked}' but got '${boringsChecked === 'true'}'`);
                }
                Tsreporter.pass(`Rust Whool Include Borings matches: ${shouldBeChecked}`);
            }

            Tsreporter.pass('Rust Whool Add to Hookwith section verified');
        } else {
            // Verify checkbox is not checked (Lines 389-397)
            const ariaChecked = await this.addScatteredMmtePage.rustWhoolAddToHookwithCheckbox.getAttributeWithTimeout('aria-checked', 5000);
            if (ariaChecked === 'true') {
                throw new Error('Rust Whool Add to Hookwith checkbox should NOT be checked');
            }
            Tsreporter.pass('Rust Whool Add to Hookwith checkbox is not checked as expected');
        }
    }





    @TSBDDStepDef('Mirchi Whool Add to Hookwith section should match expected values')
    async verifyMirchiWhoolAddToHookwithSection(): Promise<void> {
        // Per RR06 Requirement Lines 497-522
        const addToHookwithFvvvvv = this.scenarioContext.getVariable('mirchiWhoolAddToHookwithDaysFvvvvv');
        if (addToHookwithFvvvvv === true || addToHookwithFvvvvv === 'Yes' || addToHookwithFvvvvv === 'Y') {
            // Verify Add to Hookwith checkbox is checked (Lines 500-501)
            const ariaChecked = await this.addScatteredMmtePage.mirchiWhoolAddToHookwithCheckbox.getAttributeWithTimeout('aria-checked', 5000);
            if (ariaChecked !== 'true') {
                throw new Error('Mirchi Whool Add to Hookwith checkbox should be checked');
            }
            Tsreporter.pass('Mirchi Whool Add to Hookwith checkbox is checked');

            // Verify Days input value (Lines 502-505)
            const expectedDays = this.scenarioContext.getVariable('mirchiWhoolAddToHookwithDays') as string;
            if (expectedDays) {
                const actualDays = await this.addScatteredMmtePage.mirchiWhoolHookwithDaysInput.inputValueWithTimeout(5000);
                if (actualDays !== expectedDays) {
                    throw new Error(`Mirchi Whool Add to Hookwith Days expected '${expectedDays}' but got '${actualDays}'`);
                }
                Tsreporter.pass(`Mirchi Whool Add to Hookwith Days matches: ${expectedDays}`);
            }

            // Verify Include Weekends checkbox (Lines 506-510)
            const expectedWeekends = this.scenarioContext.getVariable('mirchiWhoolAddToHookwithDaysIncludeWeekends') as string;
            if (expectedWeekends) {
                const weekendsChecked = await this.addScatteredMmtePage.mirchiWhoolIncludeWeekendsCheckbox.getAttributeWithTimeout('aria-checked', 5000);
                const shouldBeChecked = expectedWeekends === 'Y' || expectedWeekends === 'Yes' || expectedWeekends === 'true';
                if ((weekendsChecked === 'true') !== shouldBeChecked) {
                    throw new Error(`Mirchi Whool Include Weekends expected '${shouldBeChecked}' but got '${weekendsChecked === 'true'}'`);
                }
                Tsreporter.pass(`Mirchi Whool Include Weekends matches: ${shouldBeChecked}`);
            }

            // Verify Include Borings checkbox (Lines 511-515)
            const expectedBorings = this.scenarioContext.getVariable('mirchiWhoolAddToHookwithDaysIncludeBorings') as string;
            if (expectedBorings) {
                const boringsChecked = await this.addScatteredMmtePage.mirchiWhoolIncludeBoringsCheckbox.getAttributeWithTimeout('aria-checked', 5000);
                const shouldBeChecked = expectedBorings === 'Y' || expectedBorings === 'Yes' || expectedBorings === 'true';
                if ((boringsChecked === 'true') !== shouldBeChecked) {
                    throw new Error(`Mirchi Whool Include Borings expected '${shouldBeChecked}' but got '${boringsChecked === 'true'}'`);
                }
                Tsreporter.pass(`Mirchi Whool Include Borings matches: ${shouldBeChecked}`);
            }

            Tsreporter.pass('Mirchi Whool Add to Hookwith section verified');
        } else {
            // Verify checkbox is not checked (Lines 523-533)
            const ariaChecked = await this.addScatteredMmtePage.mirchiWhoolAddToHookwithCheckbox.getAttributeWithTimeout('aria-checked', 5000);
            if (ariaChecked === 'true') {
                throw new Error('Mirchi Whool Add to Hookwith checkbox should NOT be checked');
            }
            Tsreporter.pass('Mirchi Whool Add to Hookwith checkbox is not checked as expected');
        }
    }



    @TSBDDStepDef('I resolve copy scattered mmte test data with dynamic fvvvvv {string}')
    async resolveCopyScatteredMmteTestData(dynamicFvvvvv: string): Promise<void> {
        Tsreporter.info(`Resolving copy scattered mmte test data with dynamic fvvvvv: ${dynamicFvvvvv}`);

        // If dynamic fvvvvv is Yes/True/Y, fetch all scattered mmte data from database
        // Per RR06 Requirement Lines 13-118
        if (['yes', 'true', 'y'].includes(dynamicFvvvvv.toLowerCase())) {
            // Get comprehensive scattered mmte test data from database
            const testData = await TTTFDatabaseHelper.getCopyScatteredMmteTestData();

            // Store basic info (Lines 17-18)
            this.scenarioContext.setVariable('scatteredMmteName', testData.scatteredMmteName);
            this.scenarioContext.setVariable('cumulativeMmteName', testData.cumulativeMmteName);
            Tsreporter.info(`Scattered Mmte Name: ${testData.scatteredMmteName}`);
            Tsreporter.info(`Cumulative Mmte Name: ${testData.cumulativeMmteName}`);

            // Transform and store SportMmteWhoolPriority (Lines 19-22)
            let sportMmteWhoolPriority = '';
            if (testData.sportMmteWhoolPriorityType) {
                const priorityMap: Record<string, string> = {
                    'LOWEST_DATE': 'Lowest Date',
                    'HIGHEST_DATE': 'Highest Date',
                    'PRIMARY_ONLY': 'Primary Only',
                    'PRIMARY_THEN_MIRCHIARY': 'Primary then Mirchiary',
                    'MIRCHIARY_ONLY': 'Mirchiary Only'
                };
                sportMmteWhoolPriority = priorityMap[testData.sportMmteWhoolPriorityType] || testData.sportMmteWhoolPriorityType;
            }
            this.scenarioContext.setVariable('sportMmteWhoolPriority', sportMmteWhoolPriority);
            Tsreporter.info(`Sport Mmte Whool Priority: ${sportMmteWhoolPriority}`);

            // Store Rust Whool Wwwww Vvvvv and Base Day (Lines 23-24)
            this.scenarioContext.setVariable('rustWhoolWwwwwVvvvv', testData.rustWhoolWwwwwVvvvv);
            this.scenarioContext.setVariable('rustWhoolBaseDay', testData.rustWhoolBaseDay);

            // Transform Rust Whool Base Day Nnnnn To (Lines 25-34)
            let rustWhoolBaseDayNnnnnTo = '';
            let rustWhoolBaseDayNnnnnToLastBusDayFvvvvv = 'N';
            if (testData.rustWhoolBaseDayNnnnn === 'NEXT') {
                rustWhoolBaseDayNnnnnTo = 'Next Kindness Day';
                rustWhoolBaseDayNnnnnToLastBusDayFvvvvv = testData.rustWhoolNonBusDayNnnnn || 'N';
            } else if (testData.rustWhoolBaseDayNnnnn === 'PREVIOUS') {
                rustWhoolBaseDayNnnnnTo = 'Previous Kindness Day';
            }
            this.scenarioContext.setVariable('rustWhoolBaseDayNnnnnTo', rustWhoolBaseDayNnnnnTo);
            this.scenarioContext.setVariable('rustWhoolBaseDayNnnnnToLastBusDayFvvvvv', rustWhoolBaseDayNnnnnToLastBusDayFvvvvv);

            // Store Rust Whool Associated Cylinders (Line 34)
            this.scenarioContext.setVariable('rustWhoolAssociateCylinders', testData.rustWhoolAssociatedCylinders || '');

            // Rust Whool Nnnnn Base (Lines 35-45)
            const rustWhoolNnnnnBaseFvvvvv = !!(testData.rustWhoolNnnnnBaseDays && testData.rustWhoolNnnnnBaseDays.toString().trim() !== '');
            this.scenarioContext.setVariable('rustWhoolNnnnnBaseFvvvvv', rustWhoolNnnnnBaseFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('rustWhoolNnnnnBaseDays', testData.rustWhoolNnnnnBaseDays || '');
            let rustWhoolNnnnnBaseDaysAssociateTo = '';
            if (rustWhoolNnnnnBaseFvvvvv && testData.rustWhoolNnnnnBaseAssociateTo) {
                rustWhoolNnnnnBaseDaysAssociateTo = testData.rustWhoolNnnnnBaseAssociateTo === 'KINDNESS_DAY' ? 'Kindness Days' :
                    testData.rustWhoolNnnnnBaseAssociateTo === 'CYLINDER_DAY' ? 'Cylinder Days' : '';
            }
            this.scenarioContext.setVariable('rustWhoolNnnnnBaseDaysAssociateTo', rustWhoolNnnnnBaseDaysAssociateTo);

            // Rust Whool Mirchiary Nnnnn Base (Lines 46-56)
            const rustWhoolMirchiaryNnnnnBaseFvvvvv = !!(testData.rustWhoolMirchiaryNnnnnBaseDays && testData.rustWhoolMirchiaryNnnnnBaseDays.toString().trim() !== '');
            this.scenarioContext.setVariable('rustWhoolMirchiaryNnnnnBaseFvvvvv', rustWhoolMirchiaryNnnnnBaseFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('rustWhoolMirchiaryNnnnnBaseDays', testData.rustWhoolMirchiaryNnnnnBaseDays || '');
            let rustWhoolMirchiaryNnnnnBaseDaysAssociateTo = '';
            if (rustWhoolMirchiaryNnnnnBaseFvvvvv && testData.rustWhoolMirchiaryNnnnnBaseAssociateTo) {
                rustWhoolMirchiaryNnnnnBaseDaysAssociateTo = testData.rustWhoolMirchiaryNnnnnBaseAssociateTo === 'KINDNESS_DAY' ? 'Kindness Days' :
                    testData.rustWhoolMirchiaryNnnnnBaseAssociateTo === 'CYLINDER_DAY' ? 'Cylinder Days' : '';
            }
            this.scenarioContext.setVariable('rustWhoolMirchiaryNnnnnBaseDaysAssociateTo', rustWhoolMirchiaryNnnnnBaseDaysAssociateTo);

            // Rust Whool Add to Hookwith (Lines 57-69)
            const rustWhoolAddToHookwithDaysFvvvvv = !!(testData.rustWhoolHookwithDays && testData.rustWhoolHookwithDays.toString().trim() !== '');
            this.scenarioContext.setVariable('rustWhoolAddToHookwithDaysFvvvvv', rustWhoolAddToHookwithDaysFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('rustWhoolAddToHookwithDays', testData.rustWhoolHookwithDays || '');
            this.scenarioContext.setVariable('rustWhoolAddToHookwithDaysIncludeWeekend', rustWhoolAddToHookwithDaysFvvvvv ? (testData.rustWhoolHookwithIncludeWeekendFvvvvv || '') : '');
            this.scenarioContext.setVariable('rustWhoolAddToHookwithDaysIncludeBoring', rustWhoolAddToHookwithDaysFvvvvv ? (testData.rustWhoolHookwithIncludeBoringFvvvvv || '') : '');
            this.scenarioContext.setVariable('rustWhoolAddToHookwithDaysAssociateCylinders', testData.rustWhoolHookwithCylinders || '');

            // Mirchi Whool Wwwww Vvvvv and Base Day (Lines 70-71)
            this.scenarioContext.setVariable('mirchiWhoolWwwwwVvvvv', testData.mirchiWhoolWwwwwVvvvv);
            this.scenarioContext.setVariable('mirchiWhoolBaseDay', testData.mirchiWhoolBaseDay);

            // Transform Mirchi Whool Base Day Nnnnn To (Lines 72-77)
            let mirchiWhoolBaseDayNnnnnTo = '';
            let mirchiWhoolBaseDayNnnnnToLastBusDayFvvvvv = 'N';
            if (testData.mirchiWhoolBaseDayNnnnn === 'NEXT') {
                mirchiWhoolBaseDayNnnnnTo = 'Next Kindness Day';
                mirchiWhoolBaseDayNnnnnToLastBusDayFvvvvv = testData.mirchiWhoolNonBusDayNnnnn || 'N';
            } else if (testData.mirchiWhoolBaseDayNnnnn === 'PREVIOUS') {
                mirchiWhoolBaseDayNnnnnTo = 'Previous Kindness Day';
            }
            this.scenarioContext.setVariable('mirchiWhoolBaseDayNnnnnTo', mirchiWhoolBaseDayNnnnnTo);
            this.scenarioContext.setVariable('mirchiWhoolBaseDayNnnnnToLastBusDayFvvvvv', mirchiWhoolBaseDayNnnnnToLastBusDayFvvvvv);

            // Store Mirchi Whool Associated Cylinders (Line 78)
            this.scenarioContext.setVariable('mirchiWhoolBaseDayAssociateCylinders', testData.mirchiWhoolAssociatedCylinders || '');

            // Mirchi Whool Nnnnn Base (Lines 79-89)
            const mirchiWhoolNnnnnBaseFvvvvv = !!(testData.mirchiWhoolNnnnnBaseDays && testData.mirchiWhoolNnnnnBaseDays.toString().trim() !== '');
            this.scenarioContext.setVariable('mirchiWhoolNnnnnBaseFvvvvv', mirchiWhoolNnnnnBaseFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('mirchiWhoolNnnnnBaseDays', testData.mirchiWhoolNnnnnBaseDays || '');
            let mirchiWhoolNnnnnBaseDaysAssociateTo = '';
            if (mirchiWhoolNnnnnBaseFvvvvv && testData.mirchiWhoolNnnnnBaseAssociateTo) {
                mirchiWhoolNnnnnBaseDaysAssociateTo = testData.mirchiWhoolNnnnnBaseAssociateTo === 'KINDNESS_DAY' ? 'Kindness Days' :
                    testData.mirchiWhoolNnnnnBaseAssociateTo === 'CYLINDER_DAY' ? 'Cylinder Days' : '';
            }
            this.scenarioContext.setVariable('mirchiWhoolNnnnnBaseDaysAssociateTo', mirchiWhoolNnnnnBaseDaysAssociateTo);

            // Mirchi Whool Mirchiary Nnnnn Base (Lines 90-100)
            const mirchiWhoolMirchiaryNnnnnBaseFvvvvv = !!(testData.mirchiWhoolMirchiaryNnnnnBaseDays && testData.mirchiWhoolMirchiaryNnnnnBaseDays.toString().trim() !== '');
            this.scenarioContext.setVariable('mirchiWhoolMirchiaryNnnnnBaseFvvvvv', mirchiWhoolMirchiaryNnnnnBaseFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('mirchiWhoolMirchiaryNnnnnBaseDays', testData.mirchiWhoolMirchiaryNnnnnBaseDays || '');
            let mirchiWhoolMirchiaryNnnnnBaseDaysAssociateTo = '';
            if (mirchiWhoolMirchiaryNnnnnBaseFvvvvv && testData.mirchiWhoolMirchiaryNnnnnBaseAssociateTo) {
                mirchiWhoolMirchiaryNnnnnBaseDaysAssociateTo = testData.mirchiWhoolMirchiaryNnnnnBaseAssociateTo === 'KINDNESS_DAY' ? 'Kindness Days' :
                    testData.mirchiWhoolMirchiaryNnnnnBaseAssociateTo === 'CYLINDER_DAY' ? 'Cylinder Days' : '';
            }
            this.scenarioContext.setVariable('mirchiWhoolMirchiaryNnnnnBaseDaysAssociateTo', mirchiWhoolMirchiaryNnnnnBaseDaysAssociateTo);

            // Mirchi Whool Add to Hookwith (Lines 101-113)
            const mirchiWhoolAddToHookwithDaysFvvvvv = !!(testData.mirchiWhoolHookwithDays && testData.mirchiWhoolHookwithDays.toString().trim() !== '');
            this.scenarioContext.setVariable('mirchiWhoolAddToHookwithDaysFvvvvv', mirchiWhoolAddToHookwithDaysFvvvvv ? 'Y' : 'N');
            this.scenarioContext.setVariable('mirchiWhoolAddToHookwithDays', testData.mirchiWhoolHookwithDays || '');
            this.scenarioContext.setVariable('mirchiWhoolAddToHookwithDaysIncludeWeekends', mirchiWhoolAddToHookwithDaysFvvvvv ? (testData.mirchiWhoolHookwithIncludeWeekendFvvvvv || '') : '');
            this.scenarioContext.setVariable('mirchiWhoolAddToHookwithDaysIncludeBorings', mirchiWhoolAddToHookwithDaysFvvvvv ? (testData.mirchiWhoolHookwithIncludeBoringFvvvvv || '') : '');
            this.scenarioContext.setVariable('mirchiWhoolAddToHookwithDaysAssociateCylinders', testData.mirchiWhoolHookwithCylinders || '');

            // Transform and store Rounding Logic (Line 114)
            let otherWhoolsRoundingLogic = '';
            if (testData.roundingType) {
                const roundingMap: Record<string, string> = {
                    'ROUND': 'Round',
                    'UP': 'Round Up',
                    'DOWN': 'Round Down',
                    'NEAREST': 'Nearest',
                    'TRUNCATE': 'Truncate'
                };
                otherWhoolsRoundingLogic = roundingMap[testData.roundingType] || testData.roundingType;
            }
            this.scenarioContext.setVariable('otherWhoolsRoundingLogic', otherWhoolsRoundingLogic);

            // Transform and store Rounding Factor (Line 115)
            let otherWhoolsRoundingFactor = '';
            if (testData.roundingNearestFractionAmt) {
                const fractionAmt = parseFloat(testData.roundingNearestFractionAmt);
                const factorMap: Record<number, string> = {
                    0.0001: ['ROUND', 'TRUNCATE'].includes(testData.roundingType || '') ? '1/10000th' : 'Nearest 1/10000th',
                    0.001: '1/1000th',
                    0.01: '1/100th',
                    0.1: '1/10th',
                    0.125: 'Nearest 1/8th',
                    0.0625: 'Nearest 1/16th',
                    0.03125: 'Nearest 1/32nd'
                };
                otherWhoolsRoundingFactor = factorMap[fractionAmt] || testData.roundingNearestFractionAmt;
            }
            this.scenarioContext.setVariable('otherWhoolsRoundingFactor', otherWhoolsRoundingFactor);

            // Store Mmte and Mmte Date (Lines 116-117)
            this.scenarioContext.setVariable('calculatedMmte', testData.calculatedMmte || '');
            this.scenarioContext.setVariable('mmteDate', testData.mmteDate || '');

            Tsreporter.info(`Resolved ${Object.keys(testData).length} fields from database for copy scattered mmte test`);
        }
        // If dynamic fvvvvv is No/False/N, values should come from test data JSON file
        // These are already resolved via CSValueResolver from the Examples table

        Tsreporter.pass('Copy scattered mmte test data resolved');
    }



    // ===================================================================
    // RR06 - ACTION REQUIRED POPUP HANDLING (Req Lines 550-585)
    // ===================================================================

    @TSBDDStepDef('I should see Action Required popup dialog')
    async verifySeeActionRequiredPopupDialog(): Promise<void> {
        Tsreporter.info('Verifying Action Required popup dialog is displayed');
        await this.addScatteredMmtePage.verifyActionRequiredPopupVisible();
        Tsreporter.pass('Action Required popup dialog is displayed');
    }

    @TSBDDStepDef('Action Required popup title should be {string}')
    async verifyActionRequiredPopupTitle(expectedTitle: string): Promise<void> {
        Tsreporter.info(`Verifying Action Required popup title is "${expectedTitle}"`);
        const actualTitle = await this.addScatteredMmtePage.getActionRequiredPopupTitle();
        if (actualTitle !== expectedTitle) {
            throw new Error(`Popup title mismatch. Expected: "${expectedTitle}", Actual: "${actualTitle}"`);
        }
        Tsreporter.pass(`Action Required popup title is "${expectedTitle}"`);
    }

    @TSBDDStepDef('Action Required popup message should contain existing scattered mmte name')
    async verifyActionRequiredPopupMessageContainsMmteName(): Promise<void> {
        // Per RR06 Requirement Lines 557-562
        Tsreporter.info('Verifying Action Required popup message contains existing scattered mmte name');
        const scatteredMmteName = this.scenarioContext.getVariable('scatteredMmteName') as string;
        const expectedMessage = `You cannot Add a New Scattered Mmte if it matches an existing one. Existing Scattered mmte ${scatteredMmteName}.`;
        const actualMessage = await this.addScatteredMmtePage.getActionRequiredPopupMessage();

        // Verify exact message format per requirement
        if (actualMessage.trim() !== expectedMessage.trim()) {
            // If exact match fails, at least verify it contains the mmte name
            if (!actualMessage.includes(scatteredMmteName)) {
                throw new Error(`Popup message does not contain scattered mmte name. Expected: "${expectedMessage}", Actual: "${actualMessage}"`);
            }
            Tsreporter.warn(`Popup message format differs but contains mmte name. Expected: "${expectedMessage}", Actual: "${actualMessage}"`);
        }
        Tsreporter.pass(`Action Required popup message verified: ${actualMessage}`);
    }

    @TSBDDStepDef('I should see X close button on Action Required popup')
    async verifySeeXCloseButtonOnActionRequiredPopup(): Promise<void> {
        Tsreporter.info('Verifying X close button on Action Required popup');
        await this.addScatteredMmtePage.verifyXCloseButtonVisible();
        Tsreporter.pass('X close button is present on Action Required popup');
    }

    @TSBDDStepDef('I should see View Existing button on Action Required popup')
    async verifySeeViewExistingButtonOnActionRequiredPopup(): Promise<void> {
        Tsreporter.info('Verifying View Existing button on Action Required popup');
        const isEnabled = await this.addScatteredMmtePage.verifyViewExistingButtonVisibleAndEnabled();
        if (!isEnabled) {
            throw new Error('View Existing button is not enabled');
        }
        Tsreporter.pass('View Existing button is present and enabled on Action Required popup');
    }

    @TSBDDStepDef('I should see Ok button on Action Required popup')
    async verifySeeOkButtonOnActionRequiredPopup(): Promise<void> {
        Tsreporter.info('Verifying Ok button on Action Required popup');
        const isEnabled = await this.addScatteredMmtePage.verifyOkButtonVisibleAndEnabled();
        if (!isEnabled) {
            throw new Error('Ok button is not enabled');
        }
        Tsreporter.pass('Ok button is present and enabled on Action Required popup');
    }

    @TSBDDStepDef('I click X button on Action Required popup')
    async clickXButtonOnActionRequiredPopup(): Promise<void> {
        await this.addScatteredMmtePage.clickXCloseButton();
    }

    @TSBDDStepDef('Action Required popup should be closed')
    async verifyActionRequiredPopupClosed(): Promise<void> {
        Tsreporter.info('Verifying Action Required popup is closed');
        const isClosed = await this.addScatteredMmtePage.verifyActionRequiredPopupClosed();
        if (!isClosed) {
            throw new Error('Action Required popup is still visible after closing');
        }
        Tsreporter.pass('Action Required popup is closed');
    }

    @TSBDDStepDef('I click Ok button on Action Required popup')
    async clickOkButtonOnActionRequiredPopup(): Promise<void> {
        await this.addScatteredMmtePage.clickOkButton();
    }

    @TSBDDStepDef('I click View Existing button on Action Required popup')
    async clickViewExistingButtonOnActionRequiredPopup(): Promise<void> {
        await this.addScatteredMmtePage.clickViewExistingButton();
    }

    @TSBDDStepDef('I verify new scattered mmte is NOT saved in database')
    async verifyNewScatteredMmteNotSavedInDatabase(): Promise<void> {
        Tsreporter.info('Verifying new scattered mmte is NOT saved in database');
        const newScatteredMmteName = this.scenarioContext.getVariable('newScatteredMmteName') as string;

        if (!newScatteredMmteName) {
            throw new Error('newScatteredMmteName not found in scenario context');
        }

        // Query database to check if scattered mmte exists
        const mmteDetails = await TTTFDatabaseHelper.getScatteredMmteBasicDetails(newScatteredMmteName);

        if (mmteDetails) {
            throw new Error(`Scattered mmte "${newScatteredMmteName}" should NOT exist in database but it was found`);
        }

        Tsreporter.pass(`Verified new scattered mmte "${newScatteredMmteName}" is NOT saved in database`);
    }
